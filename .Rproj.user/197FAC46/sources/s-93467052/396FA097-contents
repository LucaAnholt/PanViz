##reaction pair (RP) -> reactions (adjl_RP_R) adjacency list helper function:
#' adj_RP_R
#'
#' @param queried_data
#'
#' @return vector containing relevant adjacent reaction pairs and reactions
adj_RP_R <- function(queried_data){
  RP <- queried_data$RP
  return(RP)
}
